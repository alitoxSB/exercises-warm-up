package week1_programming_challenges;

import java.util.ArrayList;
import java.util.Scanner;

public class advanced_challenge_functions {

    public static void encryption_patron(){
        Scanner input = new Scanner(System.in);
        System.out.println("Write word to encrypt with this system: ");
        String word_to_encrypt = input.next();
        ArrayList<Character> arrayString = new ArrayList<>();

    }
    public String encrypt_msg(String msg_to_encrypt){
        char different_letter;
        for(int i = 0; i<msg_to_encrypt.length(); i++){
            char letter = msg_to_encrypt.charAt(i);
            different_letter = msg_to_encrypt.charAt(i);
        }
        String encrypt_msh = "";
        return encrypt_msh;
    }
}
